package my.fileManager.components;

import my.fileManager.core.File;
import my.fileManager.core.Folder;

import javax.swing.tree.DefaultMutableTreeNode;

public class TreeNodeN extends DefaultMutableTreeNode {

    /*public TreeNodeN(File item)
    {
        super(item, item instanceof Folder);
    }
/*
    public void add(TreeNode TreeNode)
    {
        add((DefaultMutableTreeNode) TreeNode);
        if (userObject instanceof Folder)
        {
            ((Folder) userObject).addFile();
            (File) TreeNode.userObject
        }
    }
    */
}
